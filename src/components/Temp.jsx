/**
 * The `Hero` component renders a wrapper div containing the `Main` component and the `ProjectShowcase`
 * component.
 * @returns The `Hero` component is being returned, which contains a `div` element with the class name
 * 'wrapper'. Inside the `div`, it renders the `Main` component and the `ProjectShowcase` component.
 */
// import React from 'react'
// import Main from "@/components/Hero"
// import ProjectShowcase from "@/components/Portfolio";

// const Hero = () => {
//   return (

//         <div className='wrapper'>
//         <Main />
//         <ProjectShowcase/>
//         </div>
//   )
// }

// export default Hero 